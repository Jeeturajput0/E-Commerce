import { Package, ShoppingCart, Users, UserSquare2 } from "lucide-react";
import { useEffect, useState } from "react";
import { api } from "../../../lib/api";
import { MetricCard, panelClass } from "../shared/adminShared";
const AdminOverview = () => { const [data, setData] = useState(null), [error, setError] = useState(""); useEffect(() => { api("/admin/dashboard").then(setData).catch((e) => setError(e.message)); }, []); if (error) return <p className="text-red-600">Dashboard failed: {error}</p>; if (!data) return <p>Loading dashboard...</p>; const cards = [["Products", data.totalProducts, Package], ["Pending products", data.pendingProducts, Package], ["Users", data.totalUsers, Users], ["Vendors", data.totalVendors, UserSquare2], ["Orders", data.totalOrders, ShoppingCart], ["Revenue", `$${data.totalRevenue}`, ShoppingCart]]; return <div className="space-y-6"><h2 className="font-display text-2xl font-semibold text-primary-600">Performance</h2><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{cards.map(([title, value, icon]) => <MetricCard key={title} title={title} value={value} delta="Live database total" icon={icon}/>)}</div><div className={panelClass}><h3 className="font-semibold">Product approval summary</h3><p className="mt-2 text-sm text-slate-600">Approved: {data.approvedProducts} · Pending: {data.pendingProducts} · Rejected: {data.rejectedProducts}</p></div></div>; };
export default AdminOverview;
